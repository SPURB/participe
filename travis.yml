language: node_js
node_js:
 - lts/*

cache:
 npm: true
 directories:
  - node_modules
  - ~/.cache

branches:
 only:
  - master

install:
 - npm install

script:
 - npm run test:unit
 - npm run test:e2e